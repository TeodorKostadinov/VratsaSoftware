@model TrySvn.Models.ClSubject

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>ClSubject</legend>

    <div class="display-label">name</div>
    <div class="display-field">@Model.name</div>

    <div class="display-label">clCategory</div>
    <div class="display-field">@Html.DisplayTextFor(_ => Model.clCategory).ToString()</div>

    <div class="display-label">clUniversity</div>
    <div class="display-field">@Html.DisplayTextFor(_ => Model.clUniversity).ToString()</div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}


